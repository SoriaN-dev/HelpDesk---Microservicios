
CREATE TABLE asset
(
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name        VARCHAR(255),
    category_id BIGINT,
    location_id BIGINT,
    CONSTRAINT pk_asset PRIMARY KEY (id)
);

CREATE TABLE category
(
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name VARCHAR(255),
    CONSTRAINT pk_category PRIMARY KEY (id)
);

CREATE TABLE location
(
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name VARCHAR(255),
    CONSTRAINT pk_location PRIMARY KEY (id)
);

ALTER TABLE asset
    ADD CONSTRAINT FK_ASSET_ON_CATEGORY FOREIGN KEY (category_id) REFERENCES location (id);

ALTER TABLE asset
    ADD CONSTRAINT FK_ASSET_ON_LOCATION FOREIGN KEY (location_id) REFERENCES category (id);